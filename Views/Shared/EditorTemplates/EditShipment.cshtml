@model SCMS.Models.Shipment

    <div class="container-fluid">
    <div class="row-fluid">

        <div class="span5">
            @*
            change
                Port
                ETA
                ETD
            Delete Button
            *@
            <div class="form-group">
                @Html.LabelFor(model => model.Amount)
            </div>
            <div class="form-group">
                @Html.TextBoxFor(model => model.Amount, new { autocomplete = "off" })
                <label id="error_amount" class="hidden" style="color:red"></label>
            </div>
        </div>
    </div>
</div>